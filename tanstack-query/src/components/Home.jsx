const Home = () => {
  return (
    <section className="flex flex-col justify-center items-center gap-2">
      <h2 className="text-4xl">Welcome Page</h2>
      <p>Post: API data fetch using fetch(), useState and useEffect()</p>
      <p>RQPost: API data fetch with React Query</p>
    </section>
  );
};

export default Home;
